function fiboEvenSum(n) {
  const arr = [1, 2];
  for (let i = 1; i < n - 1; i++) {
    let temp = arr[i] + arr[i - 1];
    if (temp <= n) arr.push(temp);
  }  
  return arr
    .filter(i => i % 2 == 0)
    .reduce((acc, curr) => acc + curr);
}
